'use client';
import React from 'react';

import TextElement from '@/components/editor/components/Text';

const EditorSidebar = () => {
	return (
		<div>
			<TextElement />
		</div>
	);
};

export default EditorSidebar;
